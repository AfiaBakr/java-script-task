git config --global user.name "User name"
git config --global user.email "User email"
 
FOR Multiple Folder in single Rapo:
git init ( Git ko initialize krny k lye)
git add .
git commit -m "Message"
git remote add origin RepoLink dalooo
git push -u origin master

FOR UPDATE EXIXTING REPOSITORY:
git add .
git commit -m "Message"
git push

TO CLONE A REPOSITORY:
cd desktop
git clone https://............
